# 2026-08-07 工作记录

## 16:43 自动化：客货邮·飞书→GitHub 回流同步
- 运行 `scripts/sync_from_feishu.py`（lark-cli user 身份，飞书已授权）。
- 结果：**无真实飞书内容变更回流**。13 个 WIKI md 内容无变化；2 张多维表格快照被重写（含 updated_at 时间戳刷新，属伪变更），但落在 `.gitignore` 的 `data/` 规则下未被 git 跟踪，commit 因无内容退出（脚本 check=True 故报错退出，非真正失败）。git push 未执行。
- 已知改进点：① 快照比对应排除 updated_at 时间戳；② 若要快照回流 GitHub，需豁免 `data/feishu_base/` 忽略规则。
